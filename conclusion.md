一、单发动机分析

1号发动机循环次数和传感器系数的相关性：

- 9、14、20、21、7、12 是负相关
- 2、3、4、8、11、13、15、17 是正相关
- 其余的没有相关性
- 其中 12、11 相关性最强，因为一个发动机具有偶然性，所以后续要考虑多个传感器

二、多发动机分析

本次分别探索了发动机 1、5、10、20 的传感器 11、12，并绘出图表（可视化）。

- 四个发动机的传感器 11 的趋势就是随着循环次数上升
- 传感器 12 则相反

三、机器学习全部流程

数据是在 train_FD001 内部划分的，数据只覆盖单一工况，而 FD001-FD004 分别对应不同工况和故障模式数量（本次仅使用了单工况单故障，结论不能直接推广到其他子集）
1. 准备数据
2. 构造标签（RUL）：循环数 = 最大循环数 - 当前循环数
3. 构造特征和标签（特征工程），标签（RUL）设置上限为 125 clipping = 125
4. 划分训练集和测试集（80/20）
5. 训练模型（RandomForest），100 棵树
6. 预测模型
7. 评估：RMSE（预测值和真实值的误差）bias 

四、结论

- 没有设置上限的时候，RMSE：41
- 设置上限后，RMSE：18
- C-MAPSS 业内经验大概在 15-25 之间，18 属于中等偏好的区间
- bias = -0.546(-代表整体低估 +代表整体高估)，高估会带来一定风险

五、散点图的观察

散点图显示预测值和真实值呈现正相关，RUL 较小时预测更准，较大时会出现系统性偏高，但是低 RUL 区间正是维护预警最需要准确的区域。

六、特征重要性

sensor_11（高压压气机出口静压）对于预测的贡献值最大（56%），与循环相关性最强相一致；sensor_7 相关性虽强但是特征重要性低，因其被 sensor_11 覆盖（特征冗余）。

七、业务总结

- 模型在接近失效期间预测准确，为发动机健康检测提供提前预警，支持维护决策
- 特征工程截断得效果显著，RMSE 从 41 降到 18

八、待优化方向
- 分区间计算RMSE bias
- 通过滑动窗口特征（rolling mean）来进一步降低 RMSE
- 尝试 XGBoost 对比效果
- 用官方 test 做最后验证
- 随机种子 random_state = 42
